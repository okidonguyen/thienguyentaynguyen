const TitleHeader = ({ title }) => {    
    return (
        <>            
            <h2 style={{padding:"5px", textAlign:"center"}}>{title}</h2>            
            <br />
        </>
    );
};

export default TitleHeader;
